name: soundmap
description: 声图 SoundMap — 录音 -> 转写 -> AI 总结 -> 思维导图
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  record: ^6.0.0
  path_provider: ^2.1.3
  file_picker: ^10.0.0
  webview_flutter: ^4.8.0
  shared_preferences: ^2.2.3
  receive_sharing_intent: ^1.8.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
